import React from 'react';
import '../App.css';

interface IProps {
    id: number
}

const Circle = (props: IProps) => {
    return (
        <div className={'item'}>

        </div>
    );
}

export default Circle;